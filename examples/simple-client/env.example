# MCP Server Domain
# Replace with your actual domain where the MCP server is deployed
MCP_DOMAIN=your-domain.com

# Optional: Timeout for HTTP requests (in seconds)
REQUEST_TIMEOUT=30

# Optional: Enable debug logging
DEBUG=false
